spring.application.name=empleados-api

# Conexión a MySQL (el contenedor empleados-mysql de compose.yaml)
spring.datasource.url=jdbc:mysql://localhost:3306/empleados_db
spring.datasource.username=academia
spring.datasource.password=academia123

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.open-in-view=false

# Swagger UI
springdoc.swagger-ui.path=/swagger-ui.html